# Scripts

Utility scripts for local dev and ops (to be added):
- `dev-seed` for creating sample tenants/projects/datasets.
- `migrate` wrapper around migration tool.
- `queue-replay` for reprocessing DLQ entries.
- `openapi-validate` to lint `docs/openapi.yaml`.
